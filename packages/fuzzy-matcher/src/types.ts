/**
 * Типы предметной области пакета нечёткого сопоставления (docs/04-matching-algorithm.md, §4.6).
 *
 * `QuerySlots` не переобъявляется здесь: пакет `text-normalizer` уже определяет его как
 * результат слотового разбора (ADR-019), поэтому `fuzzy-matcher` подключает его как
 * workspace-зависимость и импортирует тип, а не дублирует объявление. Тип реэкспортируется
 * из этого пакета (`index.ts`), чтобы агенту 2.4/4 не приходилось знать, из какого именно
 * пакета он происходит.
 */
export type { QuerySlots } from '@esim-detector/text-normalizer';

/**
 * Структурный минимум записи справочника, на котором работает сопоставление — не полная
 * схема `devices` (её вносит агент 3 в `packages/contracts`, который на момент написания
 * этого пакета пуст). Имена полей и форматы значений тождественны docs/05-data-model.md,
 * §5.3, чтобы отображение записи справочника на этот интерфейс не требовало преобразования
 * значений (только выбора подмножества полей):
 *
 * - `id` — соответствует `_id` справочника (docs/05 §5.3: строка вида `apple-iphone-15-pro`).
 *   Названо `id`, а не `_id`, поскольку внутри пакета это не идентификатор документа MongoDB,
 *   а обычное поле данных для чистой функции;
 * - `brand`, `family` — нормализованные текстовые части (docs/05 §5.3: `apple`, `galaxy-s`),
 *   ТОЛЬКО они участвуют в нечётких мерах и триграммном индексе этого пакета (AGENTS.md,
 *   предметное правило 2; docs/04 §4.2, §4.6);
 * - `generation`, `modifiers` — жёсткие ограничения (docs/04 §4.2): сравниваются точно,
 *   нечёткие меры к ним не применяются. Формат `generation: number | null` — как в справочнике
 *   (§5.3), а не `number | undefined`: поле есть всегда, просто может быть неприменимо;
 * - `modelCodes`, `aliases` — источник для точного индекса (`exact-index.ts`, docs/04 §4.6);
 * - `marketingName` — участвует в точном индексе псевдонимов наравне с `aliases` (docs/04 §4.6:
 *   «по всем маркетинговым названиям ... и псевдонимам справочника»);
 * - `popularity` — априорный вес для разрешения ничьих при ранжировании (docs/04 §4.6,
 *   таблица составляющих оценки); используется агентом 2.4, этот пакет только его переносит.
 */
export interface MatcherDevice {
  readonly id: string;
  readonly brand: string;
  readonly family: string;
  readonly generation: number | null;
  readonly modifiers: readonly string[];
  readonly modelCodes: readonly string[];
  readonly aliases: readonly string[];
  readonly marketingName: string;
  readonly popularity: number;
}

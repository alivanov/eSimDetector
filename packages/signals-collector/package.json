{
  "name": "@esim-detector/signals-collector",
  "version": "0.1.0",
  "private": true,
  "description": "Сбор сигналов устройства в браузере (UA, UA-CH, экран, WebGL, touch)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}

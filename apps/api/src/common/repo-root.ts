import { join } from 'node:path';

/**
 * Корень репозитория, вычисляемый от расположения ЭТОГО файла (образец — `tools/seed/src/defaults.ts`),
 * а не от `process.cwd()`: подъём в 4 уровня (`common` → `src`/`dist` → `api` → `apps` → корень)
 * не зависит от того, из какой директории запущен процесс, и одинаков в `ts-node-dev` (`src/`)
 * и в собранном `dist/` (`tsconfig.build.json`: `rootDir: "src"` зеркалирует структуру в `dist/`
 * без дополнительной вложенности). Другие файлы приложения импортируют эту константу, а не
 * вычисляют собственную глубину — единственное место, зависящее от расположения на диске.
 */
export const REPO_ROOT = join(__dirname, '../../../..');
